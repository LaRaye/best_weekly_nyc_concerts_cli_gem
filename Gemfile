source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in best_weekly_nyc_concerts_cli_gem.gemspec
gemspec
gem 'require_all'
